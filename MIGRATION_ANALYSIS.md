# ANALISIS MIGRASI: Frame Photo Booth dari Code-Generated ke PNG Overlay

## 1. CURRENT ARCHITECTURE

### Stack Teknologi

- **Frontend:** Next.js 16.2.10 (React 19) + Tailwind CSS 4
- **State Management:** Zustand (dengan localStorage persistence)
- **Export:** html-to-image v1.11.13 (3x pixelRatio for print quality)
- **Photo Editing:** react-easy-crop v6.2.2 (untuk crop/zoom)
- **Decorosi:** Emoji stickers (plain JavaScript, bukan SVG library)
- **Catatan:** ❌ **Tidak ada Fabric.js** - Project menggunakan DOM/CSS

### Template System Saat Ini

```javascript
{
  id: "strip-3",
  category: "PHOTOSTRIP",
  name: "Classic 3 Strip",
  width: 600,
  height: 1500,
  slots: 3,
  frameColor: "#111111",      // ← Visual (akan dihapus)
  textColor: "#FFFFFF",       // ← Visual (akan dihapus)
  slotShape: "square",        // ← Visual (akan dihapus: square, rounded, wavy, deco)
  layout: [
    { x: 50, y: 50, w: 500, h: 350 },
    { x: 50, y: 450, w: 500, h: 350 },
    { x: 50, y: 850, w: 500, h: 350 }
  ],
  stickers: [...]
}
```

### Files Terlibat Dalam Frame System

#### PRIMARY (Direct Template Usage)

1. **src/lib/templates.js**
   - Exports array of 16 templates
   - Digunakan di templates/page.js dan editor/page.js
   - Perlu: Tambah property `image`, ubah struktur `layout` → `slots`

2. **src/app/editor/page.js** (❗ KOMPLEKS - 700+ lines)
   - Rendering template preview
   - Slot rendering menggunakan `template.layout` property
   - Sticker rendering (DraggableSticker component)
   - Export logic dengan html-to-image
   - **Perlu perubahan:** Menambahkan frame PNG di render

**Ready to implement? I'll now proceed with actual code changes.**- **Time estimate:** 1-2 hours for full implementation + testing- **Risk level:** Low (preserving core logic)- **Breaking changes:** Medium (template structure)- **File changes:** 3 main files### Complexity Level- localStorage autosave- Responsive preview- All user-facing features- Export system (html-to-image)- Editor functionality (crop, drag, sticker)### What's Staying- Template structure: **frameColor/slotShape** → **image/canvas/slots**- Frame visual design: **Code** → **PNG Files**### What's Changing## SUMMARY---- ❌ Rewrite React components to hooks (already functional)- ❌ Change AI background removal- ❌ Modify camera/photobooth flow- ❌ Use Fabric.js (not needed, DOM works fine)- ❌ Change export resolution (keep 3x pixelRatio)- ❌ Migrate old sessions with old template format (keep fallback)- ❌ Add backend database for frames (just JSON in JS for now)- ❌ Implement Frame Builder admin UI yet (future phase)DON'T:## 10. NOT IN SCOPE (Don't Do)---- → All visual design dari PNG instead- Dynamic text rendering (dari template.textColor)- Code-generated slot shapes (hardcoded slotShape)- Code-generated frame colors (hardcoded frameColor)❌ **Will Be Removed:**- localStorage persistence- Responsive preview- Dark mode / styling- AI background removal feature- Photobooth camera mode- QR download flow- Export functionality (html-to-image)- Sticker system (emoji + AI image)- Slot drag-and-drop reordering- Photo cropping (react-easy-crop)✅ **Will Be Preserved:**## 9. KEY PRESERVATION POINTS---**Total Estimated Changes: ~400 lines**- ~50 lines: templates/page.js (if updating preview)- ~100 lines: editor/page.js (frame PNG rendering + layer ordering)- ~250 lines: templates.js (new template definitions)### Code Changes Scope6. **Fallback support** untuk old templates (optional)5. **Test full export** dengan new structure4. **Test individual features** (crop, drag, sticker)3. **Update slot positioning** untuk new coordinates2. **Render frame PNG** sebagai overlay di editor1. **Create new template objects** dengan PNG references### Step-by-Step## 8. IMPLEMENTATION APPROACH---- [ ] No console errors- [ ] Dev server running (next dev)- [ ] Build check (next build)- [ ] Lint check### Phase 5: Validation (10 mins)- [ ] Test QR download flow- [ ] Test sticker editing- [ ] Test photo cropping still works- [ ] Test frame PNG layer ordering (on top)- [ ] Test full flow: select template → capture → edit → export### Phase 4: Integration Test (15 mins)- [ ] Adjust any styling issues- [ ] Ensure template selection still works- [ ] Update templates/page.js preview (if needed)### Phase 3: UI Updates (15-20 mins)- [ ] Test: Photos appear di correct positions- [ ] Update slot rendering logic dengan new coordinates- [ ] Implement frame PNG rendering di editor/page.js- [ ] Update templates.js dengan new structure### Phase 2: Core Changes (30-45 mins)- [ ] Backup existing templates.js- [ ] Create new template objects dengan new structure- [ ] Create placeholder PNG files untuk testing (simple rectangles)- [ ] Create frame PNG directory structure### Phase 1: Preparation (15 mins)## 7. MIGRATION STRATEGY--- - Plan migration path untuk user sessions - Add fallback untuk missing `image` property - ✅ Tetap support old templates temporary- **Mitigation:**- **Impact:** User tidak bisa reload session dengan template lama### Risk 4: Template Lama Stop Working - Create Frame Builder UI untuk validate - Test dengan minimal 3 templates sebelum launch - Coordinate verification tool (measure PNG, verify config)- **Mitigation:**- **Impact:** Photos render di tempat yang salah### Risk 3: Koordinat PNG vs Config Mismatch - html-to-image sudah efficient, no issue - PNG files harus optimized - Use img lazy loading / preload- **Mitigation:**- **Impact:** Frame PNG loading slow, export slow### Risk 2: Performance Degradation - ✅ Keep SlotEditor, DraggableSticker logic intact - Maintain backward compatibility di templates structure sementara - Test setiap komponen secara terpisah- **Mitigation:** - **Impact:** Export, stickers, cropping stops working### Risk 1: Breaking Existing Features ⚠️## 6. RISKS & MITIGATION--- - No issue di sini ✓ - CSS `transform: scale(scaleFactor)` works dengan PNG - Gunakan existing `scaleFactor` logic- **Solution:**- **Problem:** Frame PNG perlu responsive display### Issue 6: Responsive / Mobile Preview - Export dari `templateRef` (full size) ✓ - Actual DOM element di `templateRef` tetap full size - ui code sudah handle ini dengan `scaleFactor`- **Solution:**- **Problem:** Editor preview di-scale down, export harus 1:1### Issue 5: Scaled Preview vs Actual - No issue di sini, just make sure PNG files berkualitas - html-to-image akan preserve PNG quality - PNG files harus high-res (300 DPI atau lebih)- **Solution:**- **Problem:** Frame PNG harus terlihat jernih saat export### Issue 4: Export Quality - Atau Canvas clipping saat export (html-to-image handles ini) - Gunakan CSS `overflow: hidden` pada slot elements- **Solution:**- **Problem:** Foto harus tidak melebihi slot boundary### Issue 3: Layer Clipping - Gunakan existing template dimensions sebagai reference - Untuk testing: Create simple PNG placeholder frames - Buat directory `public/frames/`- **Solution:**- **Problem:** PNG files tidak ada yet### Issue 2: Frame PNG Asset Management - ✅ **Recommended:** Mulai dengan template baru, keep lama untuk fallback - Option B: Create migration function (complex, error-prone) - Option A: Create new template objects dari awal (safer)- **Solution:** - **Problem:** Existing templates use old structure### Issue 1: Backward Compatibility## 5. CURRENT BOOTSTRAP ISSUES / CONSIDERATIONS---- src/components/ui/Button.jsx- src/lib/utils.js- src/components/QRDownloadModal.jsx (independent)- src/components/MaskEditorModal.jsx (independent)- src/app/booth/page.js (uses template.slots, will work same)### NO CHANGE - Current API tetap bisa digunakan - May add `frameImage` or `framePNG` property (optional)4. **src/store/useStore.js**### OPTIONAL (May Change) - Update coordinates to use new `slots` structure - Show PNG frame in preview (optional, dapat tetap visual-only) - Update template preview rendering3. **src/app/templates/page.js**### IMPORTANT (Should Change) - Ensure frame PNG is NOT editable (locked layer) - Keep cropping, dragging, export logic - Remove `frameColor` styling (frame comes from PNG) - Update gap/slot rendering with new `slots` structure - Render frame PNG as top layer - Biggest file (~700 lines)2. **src/app/editor/page.js** - Update slot objects to have `id`, `x`, `y`, `width`, `height` - Remove `frameColor`, `textColor`, `slotShape` - Rename `layout` → `slots` - Add `canvas` object (width, height) - Add `image` property (point to PNG files) - Transform 16 existing templates1. **src/lib/templates.js**### CRITICAL (Must Change)## 4. FILES TO MODIFY---`       └─ Show QR       ├─ Upload ke backend       ├─ Export dengan original canvas dimensions (591 × 1772)       ├─ Flatten semua layers (background + photos + stickers + frame)    └─ Export:    │  └─ Frame upload system (untuk admin/future)    │  ├─ Change background color (tergantung frame)    ├─ NEW Edit Features:    │  └─ Add emoji stickers ✓    │  ├─ Change stickers ✓    │  ├─ Drag reorder slots ✓    │  ├─ Crop/zoom foto dalam slot ✓    ├─ Edit Features (PRESERVED):    │  └─ Frame PNG layer (on top, locked, transparent areas)    │  ├─ Sticker layers (draggable)    │  ├─ Photo layers (clipped to slot dimensions)    │  ├─ Background layer (solid atau transparent)    ├─ Layer Structure:    │  └─ stickers array (decorations)    │  ├─ photos array (user images)    │  ├─ template.slots (photo positions)    │  ├─ template.image (frame PNG path)    │  ├─ template.canvas (dimensions)    ├─ Read:[EDITOR PAGE - NEW FLOW]    ↓    └─ Output: Pilih template → /booth atau /editor    │  └─ Positioning tetap menggunakan slots    │  ├─ Bisa tambahkan preview frame PNG    ├─ Render: Sama dengan sekarang, tapi:    ├─ Read: templates.js[TEMPLATES PAGE]    ↓    └─ Tidak perlu ubah code, hanya data    ├─ Save ke templates.js sebagai JSON object    │  └─ Photo slots dengan exact coordinates    │  ├─ Canvas dimensions (591 × 1772)    ├─ Admin/Developer define template object:[FRAME CONFIGURATION]    ↓    └─ Save ke public/frames/ubhara-future-leader.png    ├─ Export sebagai PNG dengan transparent areas untuk foto    ├─ Designer membuat frame di Canva/Figma/Photoshop[PNG ASSET SETUP]`### New Frame Flow`BOTTOM (Rendered First)└── Background (solid color atau transparent)│├── Photo 1├── Photo 2├── Photo 3│├── Stickers (draggable, editable)│├── Frame PNG Overlay (z-index tertinggi, locked, non-interactive)│TOP (Rendered Last)`### Layer Stacking Order (Canvas)`}  // - stickers (bisa ditambahkan nanti jika diperlukan sebagai default)  // - layout  // - slotShape  // - textColor  // - frameColor  // REMOVED:    ]    }      height: 217      width: 450,      y: 998,      x: 139,      id: "photo-3",    {    },      height: 235      width: 450,      y: 580,      x: 138,      id: "photo-2",    {    },      height: 250      width: 450,      y: 164,      x: 138,      id: "photo-1",    {  slots: [                                     // RENAME: layout → slots  // Photo slots configuration (tetap required)    },    height: 1772    width: 591,  canvas: {                                    // NEW (replace width/height)  // Canvas dimensions (actual size, tidak scaled)    image: "/frames/ubhara-future-leader.png",  // NEW  // Frame visual design    category: "PHOTOSTRIP",  name: "Future Leader",  id: "ubhara-future-leader",{`javascript### New Template Structure## 3. PROPOSED NEW ARCHITECTURE---`       └─ Show QR code untuk download       ├─ Upload ke backend       ├─ Render templateRef dengan pixelRatio=3       ├─ Validate all slots filled    └─ Export:    │  └─ Reset stickers    │  ├─ Add/edit stickers (drag, resize, delete)    │  ├─ Change frame color    │  ├─ Drag reorder slots    │  ├─ Crop/zoom foto (react-easy-crop)    ├─ Edit Features:    │  └─ Footer text (hardcoded "UBHARA JAYA")    │  ├─ Stickers (draggable)    │  ├─ Photo slots dengan positioning    │  ├─ Background color = template.frameColor    ├─ Render:    │  └─ photos array (captured images)    │  ├─ template.stickers (emoji overlay)    │  ├─ template.frameColor (background)    │  ├─ template.slotShape (visual shape)    │  ├─ template.layout (slot positions)    ├─ Read: [EDITOR PAGE]    ↓    └─ Store: Kirim ke useStore → photos array    ├─ Capture: Ambil foto sebanyak template.slots    ├─ Read: template.slots untuk validate capture count[BOOTH PAGE] (Camera)    ↓    └─ Output: Pilih template → navigate ke /booth atau /editor    ├─ Logic: frameColor sebagai background    ├─ Render: CSS preview dengan template.layout + template.slotShape    ├─ Read: templates.js (16 templates)[TEMPLATES PAGE]    ↓User Input`## 2. CURRENT FRAME FLOW--- - **Tidak perlu perubahan** - Utility functions9. **src/lib/utils.js** - **Tidak perlu perubahan** - Utility component8. **src/components/ui/Button.jsx** - **Tidak perlu perubahan** (independent) - QR code untuk download7. **src/components/QRDownloadModal.jsx** - **Tidak perlu perubahan** (independent dari frame system) - Menggunakan canvas HTML5 untuk mask editing - AI background removal editor6. **src/components/MaskEditorModal.jsx** - **Tidak perlu perubahan** (menggunakan template.slots) - Menggunakan `template.slots` untuk validate jumlah foto - Photobooth camera capture5. **src/app/booth/page.js**#### SECONDARY (Related Features) - **Tidak perlu banyak perubahan** (API tetap sama) - State: `templateId`, `photos`, `stickers`, `bgColor`4. **src/store/useStore.js** - **Perlu perubahan:** Update preview rendering untuk PNG - Menggunakan `t.layout` dan `t.slotShape` untuk CSS styling 3. **src/app/templates/page.js**

- Template preview/selection
